

const 
   assert = reuire( "assert" )
;

/* assert.ok( true ); */
describe( "Star Wars Tests", function() {
   it( "deve buscar o r2d2 com o formato correto", async () => {
      const 
         expected = [ { nome: "R2-D2", altura: "96" } ],
         nomeBase = "r2-d2"   
      ;
   } );
} );